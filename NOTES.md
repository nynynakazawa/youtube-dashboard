# YouTube Dashboard 作業メモ

## 実施�容

### 2025-11-15
- GitHub から `nynynakazawa/youtube-dashboard` をクローンし、作業用ブラン� `feature/update-dashboard` を作��
- 連絡用の `NOTES.md` を用意し、以降�タスクログ置き場として運用開始�
- フロントエンド実�� codeX へ委譲できるよう、`docs/plan.md` に Next.js/Tailwind/TanStack Query/Recharts/React Query などの技術選定、API 仕様、画面要件、DoD、タスクプロンプトを詳細に記述�
- バックエンド担当（ざわ）向けに、Lambda・RDS・DynamoDB・YouTube Data API・Streamlit の責務と�ィレクトリ構�、環�変数�進め方をまとめた `docs/zawa-backend-spec.md` を作��
- 上記ドキュメント�すべて `feature/update-dashboard` ブランチでコミット�プッシュ済み。フロン�/バック双方� `docs/` 配下� md を参照すればすぐ着手できる状態�

## 共有事�・Next Action 依頼
- `docs/plan.md`��フロント計画��と `docs/zawa-backend-spec.md`��バ�クエンド計画����容確認をお願いします。仕様に追加・修正が�要であればコメントが欲しいです�
- codeX へフロント実�を投げる場合�、docs/plan.md のマスタープロンプトとタスクプロンプトの手�で進めてもらえるか確認したいです�
- バックエンド�で RDS スキーマ定義� Lambda 実�に着手してよいか、優先�位や�当��につ�て�示をください�
- 他に�要なドキュメント（�: Streamlit 側の仕様、GitHub Projects でのタスク整�など��があればリクエストをお願いします�

## 共有済みフロント�ネクストアクション��優先頼
1. **API 基盤整�**  
   - `.env.local` に `NEXT_PUBLIC_API_BASE_URL=https://{api-id}.execute-api.{region}.amazonaws.com/prod` を追加し、`lib/api.ts` を�行作�して API 呼び出し�入口を統一する�  
   - `importChannel` / `getChannels` / `getChannelVideos` などはこ�モジュール経由で提供し、バ�クエンド完�前でもモ�ク差し替えを容易にする�
2. **チャンネル登録フォーム�/channels/import�**  
   - `app/(routes)/channels/import/page.tsx` を作�し、チャンネル URL/ID 入力�送信ボタン、ロー�ィング表示、エラー表示を実��  
   - `lib/api.ts` の `importChannel` を呼び、�功時�同��ジ�にチャンネル�報��サマリカードを表示��後で詳細ペ�ジ遷移に拡張予定）�
3. **チャンネル一覧ペ�ジ�/channels�**  
   - `app/(routes)/channels/page.tsx` を用意し、`getChannels`��当面はモ�ク��で取得したデータをカー�/�ーブル表示�  
   - 行クリ�クで `/channels/[id]/videos` へ遷移できるようにし、UX の導線を固定する�
4. **動画一覧ペ�ジの土台�/channels/[id]/videos�**  
   - URL 構造だけでも決めて `app/(routes)/channels/[id]/videos/page.tsx` を作��  
   - チャンネル名（ダミ㼼�＋テーブル��ソー�/フィルタ UI を�置し、後で `getChannelVideos` を差し込めるようにする�
5. **グラフ用コンポ�ネント�雛形**  
   - `components/features/charts/TopVideosChart.tsx` と `MonthlyViewsChart.tsx` を作り、Recharts でダミ��ータを描画しておく�  
   - props は `Video[]` を受け取る想定にし、「��を渡せ�フロントで可視化まで完結する」契�を敷�ておく�

### 今日〜数日で進めた�タスク候�
- `.env.local` と `lib/api.ts` の整�
- `/channels/import` ペ�ジ�フォームコンポ�ネント実�
- `/channels` 一覧ペ�ジのモ�ク実�

※ ここまで完�すれば、ざわ�は Lambda/RDS 実�に�中でき、React 側のルー�ィングも固まるため、codeX へのタスク�り�しが容易になります�
### 2025-11-16
- Reset eature/update-dashboard to the latest origin state and merged changes from zawa_backend.
- UI work will proceed in order: /channels/import �� /channels �� /channels/[id] �� /channels/[id]/videos.
- Ask whenever something is unclear.
